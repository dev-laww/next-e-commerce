import { NextApiRequest } from "next";
import { {{pascalCase name}}Controller } from "@controllers/{{name}}.controller";


describe("{{pascalCase name}}Controller", () => {
    it("should return a message", async () => {
        const controller = new {{pascalCase name}}Controller();

        const req: Partial<NextApiRequest> = {
            method: "GET",
        };

        const {status, data} = await controller.hello(req as NextApiRequest);

        expect(data).toEqual({ message: "Hello World" });
        expect(status).toEqual(200);
    });
});

